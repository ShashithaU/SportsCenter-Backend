spring:
  data:
    mongodb:
      uri: mongodb+srv://udeshshashitha:1GLEyi9MzXp9BpHS@cluster0.r4whh.mongodb.net/sports-center
      # Alternatively, specify individual properties
      # host: localhost
      # port: 27017
      # database: database_name
      # username: username
      # password: password
  application:
    name: your-application-name
